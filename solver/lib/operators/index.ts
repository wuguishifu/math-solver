import frac from './frac';
import sqrt from './sqrt';

import plus from './+';
import minus from './-';
import cdot from './cdot';
import pow from './^';

export default {
    '+': plus,
    '-': minus,
    '^': pow,
    cdot,

    frac,
    sqrt,
}