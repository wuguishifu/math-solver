import frac from './frac';
import sqrt from './sqrt';

import plus from './+';
import minus from './-';
import cdot from './cdot';

export default {
    '+': plus,
    '-': minus,
    cdot,

    frac,
    sqrt,
}